{
  "id": "03a0eca6",
  "title": "Fix workflow discovery edge cases",
  "tags": [
    "actioneer",
    "rewrite",
    "discovery"
  ],
  "status": "closed",
  "created_at": "2026-06-18T16:59:31.017Z"
}

Complete workflow discovery to match SPEC edge cases.

Required:
- reusable workflow `jobs.<name>.uses`
- composite action `steps[*].uses`
- action paths: `owner/repo/path@ref`
- quoted and unquoted YAML scalar values
- version comments: `# v1.2.3`
- preserve file, line, and edit location for patching
- ignore local refs (`./`, `../`)
- ignore docker refs (`docker://`)
- ignore non-YAML files when scanning directories
- handle `action.yml` / `action.yaml` for composite actions
- avoid matching `uses:` strings that are not actual `uses:` keys

Add tests for each edge case.
Keep tests green.
