{
  "id": "31b7e50f",
  "title": "Add gh auth token fallback",
  "tags": [
    "actioneer",
    "rewrite",
    "github"
  ],
  "status": "closed",
  "created_at": "2026-06-18T18:46:23.318Z"
}

- In `src/github.rs`, add `gh auth token` fallback when `GITHUB_TOKEN` env var is not set.
- Use `std::process::Command` to run `gh auth token` non-interactively.
- If `gh` is not available or fails, proceed without auth.
- Keep tests green; existing tests use env var only.
