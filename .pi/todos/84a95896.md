{
  "id": "84a95896",
  "title": "Separate update state and enforce mode gating",
  "tags": [
    "rewrite",
    "refactor",
    "update"
  ],
  "status": "in-progress",
  "created_at": "2026-06-14T17:10:27.910Z"
}

Address review against updated SPEC:

- Candidate data must not carry selection/application state.
- Patch layer should receive patch edits, not mutable candidates.
- `plain`/`json` update modes must require `--yes` or `--dry-run` and fail before GitHub/cache fetching when possible.

Use behavior test for mode gating, then refactor while keeping tests green.
