{
  "id": "b3303106",
  "title": "Clean up ActionRef compatibility alias and Candidate state",
  "tags": [
    "actioneer",
    "rewrite",
    "data-model"
  ],
  "status": "closed",
  "created_at": "2026-06-18T16:58:21.532Z"
}

Finish the pre-TUI data model cleanup.

Tasks:
- Remove `pub type DiscoveredActionRef = ActionRef;` alias if nothing needs it.
- Move `selected` and `applied` out of `Candidate`.
  - `Candidate` should represent only the planned update data.
  - `selected` indexes should be UI/command-local.
  - `applied` results should be patch/output-local.
- Stop `Candidate` from carrying `kind()`/`reason()` output helpers.
  - Keep JSON strings in `update/output.rs` unless real domain enums are needed.

Goal: TUI should receive `&[Candidate]` and return `Vec<usize>`, not mutate candidate state.
Keep tests green.
