name: discovery

on:
  push:

jobs:
  call-reusable:
    uses: luxass/reusable/.github/workflows/ci.yml@v1

  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4 # v4.2.2
      - uses: "owner/tool/path@main"
      - uses: ./local-action
      - uses: ../parent-action
      - uses: docker://alpine:3.20
      - run: echo "uses: not/an/action@v1"
